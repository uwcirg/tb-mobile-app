import React from 'react';
import Box from '@material-ui/core/Box'

const ContentContainer = (props) => {

    return(<Box minHeight={"50vh"} padding="1em" {...props}  />);

}

export default ContentContainer;