import { Typography, withStyles } from "@material-ui/core";

const SubSectionTitle = withStyles({
  root: {
    fontSize: "1em",
    fontWeight: "700",
  },
})(Typography);

export default SubSectionTitle;
